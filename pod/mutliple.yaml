apiVersion: v1
kind: Pod
metadata:
  name: multiple
spec:
  containers:
  - image: nginx
    name: nginx
    ports:
      containerport: 80
  - image: almalinux
    name: alamalinux
  command: ["sleep","20"]